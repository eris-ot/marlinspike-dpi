[package]
name = "fathom-contracts"
version = "0.1.0"
edition = "2024"
license = "Proprietary"
description = "Shared types and contracts between Fathom appliance, collectors, and pluggable engines"
publish = false

[lib]
name = "fathom_contracts"

[dependencies]
serde = { version = "1", features = ["derive"] }

[dev-dependencies]
serde_json = "1"
